worker_processes auto;

events {
    worker_connections 1024;
}

stream {
    upstream deltaplay_server {
        server server:8080;
    }

    server {
        listen 443;
        proxy_pass deltaplay_server;
        proxy_timeout 100s;
        proxy_connect_timeout 5s;
    }
}